# Copy to keystore.properties and keep the real file outside Git.
# storeFile may be an absolute path or a path relative to mobile/android.
storeFile=/absolute/path/to/yeojju-upload.jks
storePassword=replace-with-secret
keyAlias=upload
keyPassword=replace-with-secret
